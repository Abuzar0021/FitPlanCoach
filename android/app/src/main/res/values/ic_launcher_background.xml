<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Brand black: the adaptive foreground is the FitPlanCoach bolt on a
         black plate, so the background must match or the mask rings it white. -->
    <color name="ic_launcher_background">#000000</color>
</resources>